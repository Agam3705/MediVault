services:
  # 1. Web Service (Laravel Application)
  - type: web
    name: medivault-web
    env: docker
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
